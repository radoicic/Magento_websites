define([
    'Magento_Ui/js/form/element/select'
], function (Element) {
    'use strict';

    return Element.extend({
        defaults: {
            template: 'ui/form/field'
        }
    });
});
